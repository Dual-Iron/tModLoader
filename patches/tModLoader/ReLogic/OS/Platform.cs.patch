--- src/Terraria/ReLogic/OS/Platform.cs
+++ src/tModLoader/ReLogic/OS/Platform.cs
@@ -1,3 +_,4 @@
+using ReLogic.Localization.IME;
 #if WINDOWS
 using ReLogic.OS.Windows;
 #elif LINUX
@@ -40,7 +_,9 @@
 			_services.Register(service);
 		}
 
-		public abstract void InitializeClientServices(IntPtr windowHandle);
+		public void InitializeClientServices() {
+			RegisterService((IImeService)new FnaIme());
+		}
 
 		public static T Get<T>() where T : class => Current._services.Get<T>();
 		public static bool Has<T>() where T : class => Current._services.Has<T>();
